require "spec_helper"

RSpec.describe Capistrano::Dingtalk do
  it "has a version number" do
    expect(Capistrano::Dingtalk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
